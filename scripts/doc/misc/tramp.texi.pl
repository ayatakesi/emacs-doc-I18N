#!/usr/bin/perl
# This script requires libintl-perl(>=1.09).
use Locale::TextDomain ("/data/data/com.termux/files/home/gitroot/emacs-doc-I18N/doc/misc/tramp.texi" => "/data/data/com.termux/files/home/gitroot/emacs-doc-I18N/po/gettext/doc/misc");
my ($en, $ja);
while (<>) {
	($en, $ja) = (quotemeta('@chapter An overview of @value{tramp}'), __ '@chapter An overview of @value{tramp}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Obtaining @value{tramp}'), __ '@chapter Obtaining @value{tramp}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Short introduction how to use @value{tramp}'), __ '@chapter Short introduction how to use @value{tramp}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section File name syntax'), __ '@section File name syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @option{ssh} and @option{plink}'), __ '@section Using @option{ssh} and @option{plink}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @option{su}, @option{sudo} and @option{sg}'), __ '@section Using @option{su}, @option{sudo} and @option{sg}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Combining @option{ssh} or @option{plink} with @option{su} or @option{sudo}'), __ '@section Combining @option{ssh} or @option{plink} with @option{su} or @option{sudo}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @command{sudoedit}'), __ '@section Using @command{sudoedit}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @command{smbclient}'), __ '@section Using @command{smbclient}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @acronym{GVFS}-based methods'), __ '@section Using @acronym{GVFS}-based methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @acronym{GNOME} Online Accounts based methods'), __ '@section Using @acronym{GNOME} Online Accounts based methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using Android'), __ '@section Using Android'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using @command{rclone}'), __ '@section Using @command{rclone}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Configuring @value{tramp}'), __ '@chapter Configuring @value{tramp}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Types of connections to remote hosts'), __ '@section Types of connections to remote hosts'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Inline methods'), __ '@section Inline methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section External methods'), __ '@section External methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section @acronym{GVFS}-based external methods'), __ '@section @acronym{GVFS}-based external methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Selecting a default method'), __ '@section Selecting a default method'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Which method to use?'), __ '@subsection Which method to use?'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Selecting a default user'), __ '@section Selecting a default user'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Selecting a default host'), __ '@section Selecting a default host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Connecting to a remote host using multiple hops'), __ '@section Connecting to a remote host using multiple hops'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Passing firewalls'), __ '@section Passing firewalls'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Tunneling with ssh'), __ '@subsection Tunneling with ssh'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Tunneling with PuTTY'), __ '@subsection Tunneling with PuTTY'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Using Non-Standard Methods'), __ '@section Using Non-Standard Methods'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Selecting config files for user/host name completion'), __ '@section Selecting config files for user/host name completion'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Reusing passwords for several connections'), __ '@section Reusing passwords for several connections'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Using an authentication file'), __ '@subsection Using an authentication file'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Caching passwords'), __ '@subsection Caching passwords'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Reusing connection related information'), __ '@section Reusing connection related information'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Setting own connection related information'), __ '@section Setting own connection related information'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Relevant connection properties to override'), __ '@subsection Relevant connection properties to override'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section How @value{tramp} finds and uses programs on the remote host'), __ '@section How @value{tramp} finds and uses programs on the remote host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Remote shell setup hints'), __ '@section Remote shell setup hints'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Changing the default remote or local shell'), __ '@subsection Changing the default remote or local shell'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Other remote shell setup hints'), __ '@subsection Other remote shell setup hints'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Android shell setup hints'), __ '@section Android shell setup hints'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Auto-save and Backup configuration'), __ '@section Auto-save and Backup configuration'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Issues with Cygwin ssh'), __ '@section Issues with Cygwin ssh'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Using @value{tramp}'), __ '@chapter Using @value{tramp}'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section @value{tramp} file name conventions'), __ '@section @value{tramp} file name conventions'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Alternative file name syntax'), __ '@section Alternative file name syntax'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section File name completion'), __ '@section File name completion'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Declaring multiple hops in the file name'), __ '@section Declaring multiple hops in the file name'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Integration with other Emacs packages'), __ '@section Integration with other Emacs packages'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running remote programs that create local X11 windows'), __ '@subsection Running remote programs that create local X11 windows'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running @code{shell} on a remote host'), __ '@subsection Running @code{shell} on a remote host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running @code{shell-command} on a remote host'), __ '@subsection Running @code{shell-command} on a remote host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running @code{eshell} on a remote host'), __ '@subsection Running @code{eshell} on a remote host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running a debugger on a remote host'), __ '@subsection Running a debugger on a remote host'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection Running remote processes on MS Windows hosts'), __ '@subsection Running remote processes on MS Windows hosts'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Cleanup remote connections'), __ '@section Cleanup remote connections'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Renaming remote files'), __ '@section Renaming remote files'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Archive file names'), __ '@section Archive file names'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Reporting Bugs and Problems'), __ '@chapter Reporting Bugs and Problems'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter Frequently Asked Questions'), __ '@chapter Frequently Asked Questions'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter How file names, directories and localnames are mangled and managed'), __ '@chapter How file names, directories and localnames are mangled and managed'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Splitting a localname into its component parts'), __ '@section Splitting a localname into its component parts'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@section Integrating with external Lisp packages'), __ '@section Integrating with external Lisp packages'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection File name completion.'), __ '@subsection File name completion.'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@subsection File attributes cache.'), __ '@subsection File attributes cache.'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@chapter How to Customize Traces'), __ '@chapter How to Customize Traces'); s/$en/$ja/;
	($en, $ja) = (quotemeta('@appendix GNU Free Documentation License'), __ '@appendix GNU Free Documentation License'); s/$en/$ja/;
	print;
}